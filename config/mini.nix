{
  plugins.mini = {
    enable = true;
    mockDevIcons = true;
    modules = {
      icons = { };
      pairs = { };
      surround = { };
      comment = { };
      git = { };
      diff = { };
    };
  };
}
